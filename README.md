# Evaluating-RAG-systems
building a process for evaluating different rag systems, in terms of two main apsects, the retrieval, and the generation

This project is configured to use local Ollama for generation.
Default chat model: `llama3.2`
Default Ollama URL: `http://127.0.0.1:11434`
